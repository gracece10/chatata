import 'package:flutter/material.dart';

class ChatBarberContent extends StatelessWidget {
  const ChatBarberContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      "Konten untuk Chat Barber",
      style: TextStyle(
        fontSize: 18,
        fontWeight: FontWeight.bold,
      ),
    );
  }
}
