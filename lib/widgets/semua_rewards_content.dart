import 'package:flutter/material.dart';

class SemuaRewardsContent extends StatelessWidget {
  const SemuaRewardsContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text(
          "Konten untuk Semua",
          style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
        ),
      ],
    );
  }
}
